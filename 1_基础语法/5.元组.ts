// 元祖: 有更强的约束作用
// const teacherInfo: [string, string, number] = ['Dell', 'male', 24]

// 以元组为单位的数组

/* const teacherList: [string, string, number][] = [
  ['Lisa', 'female', 24],
  ['Jason', 'male', 30],
] */
